import 'package:flutter/material.dart';

class ReadPdfviewer extends StatelessWidget {
  const ReadPdfviewer({super.key});

  @override
  Widget build(BuildContext context) {
    return FractionallySizedBox(
      widthFactor: 0.95,
      heightFactor: 0.95,
      child:
          Center(child: Text("aqui se implementara la funcion de leer libros")),
    );
  }
}
